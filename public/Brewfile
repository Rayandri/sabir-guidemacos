# Brewfile - setup Mac de Sabir (par Rayan)
# Usage :  brew bundle --file=Brewfile
# (Amphetamine, Mac Mouse Fix et Hidden Bar ne sont pas ici : App Store / optionnels.)

# --- Police (obligatoire pour Powerlevel10k) ---
cask "font-fira-code-nerd-font"

# --- Terminal ---
cask "iterm2"

# --- Dev : outils CLI ---
brew "git"
brew "gh"
brew "nvm"
brew "pyenv"
brew "neovim"
brew "bat"
brew "tree"
brew "htop"
brew "wget"
brew "coreutils"

# --- Dev : apps ---
cask "visual-studio-code"
cask "cursor"
cask "claude"
cask "docker-desktop"
# Claude Code (CLI) s'installe via l'installeur officiel, pas via brew :
#   curl -fsSL https://claude.ai/install.sh | bash

# --- Utilitaires Mac (confort) ---
cask "rectangle"
cask "alt-tab"
cask "macs-fan-control"
cask "stats"
cask "appcleaner"
